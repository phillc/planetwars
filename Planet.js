exports.Planet = function(id, x, y, owner, ships, growth) {
    this.id     = id;
    this.x      = x;
    this.y      = y;
    this.owner  = owner;
    this.ships  = ships;
    this.growth = growth;
}
