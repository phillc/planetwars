var vows = require('vows'),
    assert = require('assert'),
    sys = require('sys');

var evaluations = require('../evaluations'),
    players = require('../players');
    
vows.describe('evaluations').addBatch({
    '' : {
    }
});
